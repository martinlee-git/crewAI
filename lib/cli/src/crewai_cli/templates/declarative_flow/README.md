# 읽어보기

- 원문 저장소: `crewAIInc/crewAI`
- 미러 저장소: `martinlee-git/crewAI`
- 원문 문서: https://github.com/crewAIInc/crewAI/blob/main/lib/cli/src/crewai_cli/templates/declarative_flow/README.md
- 미러 경로: `lib/cli/src/crewai_cli/templates/declarative_flow/README.md`

## 한글 요약

{{name}} Flow 이 프로젝트는 src/{{folder name}}/flow.yaml에 선언적 CrewAI Flow를 정의합니다. 설치 실행 src/{{폴더 이름}}/flow.yaml에서 선언적 흐름 정의를 편집하여 흐름을 변경합니다. src/{{폴더 이름}}/crews/에 재사용 가능한 팀을 추가하고, src/{{폴더 이름}}/tools/에 사용자 정의 Python 도구를 추가하고, src/{{폴더 이름}}/knowledge/에 공유 지식 파일을 추가합니다.

## 핵심 발췌

{{name}} Flow 이 프로젝트는 src/{{folder name}}/flow.yaml에 선언적 CrewAI Flow를 정의합니다. ## 설치 ## 실행 `src/{{에서 선언적 흐름 정의를 편집합니다.

## 원문 내용

# {{name}} Flow

This project defines a declarative CrewAI Flow in `src/{{folder_name}}/flow.yaml`.

## Install

```bash
crewai install
```

## Run

```bash
crewai run
```

Edit the declarative flow definition at `src/{{folder_name}}/flow.yaml` to change the flow. Add reusable crews under `src/{{folder_name}}/crews/`, custom Python tools under `src/{{folder_name}}/tools/`, and shared knowledge files under `src/{{folder_name}}/knowledge/`.